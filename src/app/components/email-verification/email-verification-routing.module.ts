import { Routes } from '@angular/router';
import { EmailVerificationComponent } from './email-verification.component';
 
export const EmailVerififRoutes: Routes = [
	{ 
		path: 'email-verification', 
		component: EmailVerificationComponent
    }
];