import { TarefaFavDirective } from './tarefa-fav.directive';

describe('TarefaFavDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefaFavDirective();
    expect(directive).toBeTruthy();
  });
});
